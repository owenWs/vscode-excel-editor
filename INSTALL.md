# Excel Editor 插件安装使用手顺

## 一、安装插件

### 1. 下载 VSIX 文件

前往插件发布页面下载最新版本：

👉 https://github.com/owenWs/vscode-excel-editor/releases

点击最新 Release 下的 `vscode-excel-editor-1.0.0.vsix` 文件进行下载。

---

### 2. 在 VS Code 中安装

**方法 A（推荐）：**

1. 打开 VS Code
2. 按 `Ctrl+Shift+P` 打开命令面板
3. 输入 `Install from VSIX`，选择 **Extensions: Install from VSIX...**
4. 选择刚才下载的 `.vsix` 文件
5. 安装完成后点击 **Reload Window** 重载窗口

**方法 B（命令行）：**

```bash
code --install-extension vscode-excel-editor-1.0.0.vsix
```

---

## 二、使用方法

安装完成后，在 VS Code 中直接打开任意 `.xlsx`、`.xlsm`、`.xls` 文件，插件自动激活，无需额外配置。

---

### 基本操作

| 操作 | 方式 |
|------|------|
| 选中单元格 | 单击 |
| 开始编辑 | 双击 / `Enter` / `F2` / 直接输入 |
| 提交编辑 | `Enter` |
| 取消编辑 | `Esc` |
| 清空内容 | `Delete` / `Backspace` |
| 保存文件 | `Ctrl+S` |
| 移动单元格 | 方向键 / `Tab` |

---

### 行 / 列操作

| 操作 | 方式 |
|------|------|
| 选中整行 | 点击左侧行号 |
| 选中整列 | 点击顶部列头（A / B / C…） |
| 追加多选 | `Ctrl+Click` 行号 / 列头 |
| 范围选中 | `Shift+Click` 行号 / 列头 |
| 复制选中行/列 | `Ctrl+C` |
| 粘贴到目标位置前 | 选中目标行/列后按 `Ctrl+V` |
| 插入空行/列 | 工具栏按钮 |
| 删除行/列 | 右键菜单 → 删除 |

---

### 工作表操作

| 操作 | 方式 |
|------|------|
| 切换工作表 | 点击底部 Tab |
| 重命名工作表 | 双击 Tab / 右键 → 重命名 |
| 新增工作表 | 工具栏 **＋ 工作表** |
| 复制工作表 | 工具栏 **复制工作表** / 右键 Tab → 复制 |
| 删除工作表 | 右键 Tab → 删除工作表 |

---

### 右键菜单

- **单元格**：复制 / 粘贴 / 清空 / 插入行列 / 删除行列
- **行头 / 列头**：复制、粘贴、插入、删除（支持多选批量操作）
- **Sheet Tab**：重命名、复制工作表、删除工作表

---

## 三、自动更新

插件启动时会自动检测 GitHub 是否有新版本：

- 有新版本时弹出提示，点击 **立即更新** 自动下载并安装
- 安装完成后点击 **重载窗口** 生效
- 手动检查：`Ctrl+Shift+P` → **Excel Editor: 检查更新**

---

## 四、注意事项

- 支持格式：`.xlsx`、`.xlsm`（含 VBA 宏）、`.xls`
- **VBA 宏完整保留**，保存后不会丢失
- 暂不支持：单元格样式（颜色/字体/边框）、合并单元格、图表、Undo/Redo
- 大文件（> 10 MB）加载可能稍慢，请耐心等待

---

## 五、卸载

打开扩展面板（`Ctrl+Shift+X`）→ 找到 **Excel Editor** → 齿轮图标 → **卸载**
