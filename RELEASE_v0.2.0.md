# XLSM Editor v0.2.0 发布说明

发布日期：2026-03-20

## 🎉 新功能

### 1. 复制行功能
现在可以将任意行复制到工作表末尾，支持批量复制。

**使用方法：**
1. 选中要复制的行中的任意单元格
2. 在"复制选中行"输入框输入复制次数（默认为 1）
3. 点击"复制行"按钮
4. 该行的所有内容（包括格式、公式）将被复制到表格末尾

**适用场景：**
- 快速创建相似数据行
- 模板行批量复制
- 测试数据生成

### 2. 复制列功能
现在可以将任意列复制到工作表末尾，支持批量复制。

**使用方法：**
1. 选中要复制的列中的任意单元格
2. 在"复制选中列"输入框输入复制次数（默认为 1）
3. 点击"复制列"按钮
4. 该列的所有内容（包括格式、公式）将被复制到表格末尾

**适用场景：**
- 扩展数据表结构
- 创建相似的数据列
- 批量添加计算列

### 3. 复制工作表功能
一键复制整个工作表，包含所有数据、格式和公式。

**使用方法：**
1. 切换到要复制的工作表
2. 点击工具栏的"复制工作表"按钮
3. 新工作表将自动创建，名称为 "原名称 (2)"、"原名称 (3)" 等

**适用场景：**
- 创建工作表备份
- 基于现有工作表创建新变体
- 快速复制模板结构

## 🔧 技术改进

- ✅ 完整的单元格深拷贝，确保数据独立性
- ✅ 保留单元格格式（数字、字符串、布尔值）
- ✅ 保留公式和公式字符串
- ✅ 保留 VBA 宏不受影响
- ✅ 自动更新工作表范围，确保数据完整性
- ✅ 智能命名避免工作表名称冲突
- ✅ 完善的错误处理和用户反馈

## 📦 安装

### 从 VSIX 文件安装
```bash
code --install-extension vscode-xlsm-editor-0.2.0.vsix
```

或在 VS Code 中：
1. 按 `Ctrl+Shift+P` (Mac: `Cmd+Shift+P`)
2. 输入 "Extensions: Install from VSIX..."
3. 选择 `vscode-xlsm-editor-0.2.0.vsix` 文件

### 升级说明
如果已安装 v0.1.0，建议先卸载旧版本再安装新版本：
1. 在扩展面板中找到 "XLSM Editor"
2. 点击卸载
3. 重启 VS Code
4. 安装 v0.2.0

## 📝 已知限制

- 复制操作会将新行/列添加到末尾，暂不支持在指定位置插入
- 暂不支持 Undo/Redo（基础框架已就绪）
- 不会复制单元格样式（颜色、字体等），只复制数据和公式

## 🐛 问题反馈

如遇到问题，请在 GitHub 提 Issue 或联系开发者。

## 🙏 致谢

感谢所有用户的反馈和支持！

---

**文件信息：**
- 版本：v0.2.0
- 文件大小：901 KB
- 文件数：10 个
- 支持格式：.xlsm, .xlsx, .xls
- VS Code 版本要求：^1.80.0
